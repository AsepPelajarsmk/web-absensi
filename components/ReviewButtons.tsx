'use client';import { useRouter } from 'next/navigation';export function ReviewButtons({id}:{id:string}){const router=useRouter();async function review(approved:boolean){await fetch('/api/attendance/review',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({id,approved})});router.refresh()}return <span className="actions"><button className="btn green" onClick={()=>review(true)}>Setujui</button><button className="btn red" onClick={()=>review(false)}>Tolak</button></span>}
